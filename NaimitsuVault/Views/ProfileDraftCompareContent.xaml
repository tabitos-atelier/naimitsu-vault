<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="NaimitsuVault.Views.ProfileDraftCompareContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:loc="using:NaimitsuVault.Localization"
    Padding="20,16">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid
            Grid.Row="0"
            VerticalAlignment="Center"
            ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <FontIcon
                Grid.Column="0"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                FontSize="{StaticResource NvIconSizeDefault}"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Glyph="&#xE8EC;" />

            <StackPanel
                Grid.Column="1"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Orientation="Horizontal"
                Spacing="6">
                <FontIcon
                    FontSize="{StaticResource NvIconSizeDefault}"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Glyph="&#xE70F;" />
                <TextBlock
                    x:Name="DraftHeaderLabel"
                    VerticalAlignment="Center"
                    FontSize="{StaticResource NvBodyFontSize}"
                    FontWeight="SemiBold"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>

            <StackPanel
                Grid.Column="2"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Orientation="Horizontal"
                Spacing="6">
                <FontIcon
                    FontSize="{StaticResource NvIconSizeDefault}"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Glyph="&#xE7C1;" />
                <TextBlock
                    x:Name="Gen0HeaderLabel"
                    VerticalAlignment="Center"
                    FontSize="{StaticResource NvBodyFontSize}"
                    FontWeight="SemiBold"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>
        </Grid>

        <Rectangle
            Grid.Row="1"
            Height="1"
            Margin="0,4,0,8"
            Fill="{ThemeResource DividerStrokeColorDefaultBrush}" />

        <!--  Field rows (added from code-behind)  -->
        <ScrollViewer Grid.Row="2" MaxHeight="380">
            <StackPanel x:Name="FieldRowsPanel" Spacing="6" />
        </ScrollViewer>

        <Rectangle
            Grid.Row="3"
            Height="1"
            Margin="0,8,0,8"
            Fill="{ThemeResource DividerStrokeColorDefaultBrush}" />

        <!--  Bottom bar: eye toggle on the left edge, discard + close on the right edge  -->
        <Grid Grid.Row="4">
            <Button
                x:Name="RevealToggleBtn"
                HorizontalAlignment="Left"
                Click="ToggleReveal_Click"
                ToolTipService.ToolTip="{loc:Msg Key=Common.ToggleVisibility}"
                Visibility="Collapsed">
                <FontIcon
                    x:Name="RevealIcon"
                    FontSize="{StaticResource NvIconSizeDefault}"
                    Glyph="&#xE7B3;" />
            </Button>

            <StackPanel
                HorizontalAlignment="Right"
                Orientation="Horizontal"
                Spacing="8">
                <Button Click="Discard_Click" Foreground="{ThemeResource SystemFillColorCriticalBrush}">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon FontSize="{StaticResource NvIconSizeDefault}" Glyph="&#xE75C;" />
                        <TextBlock Text="{loc:Msg Key=Common.Discard}" />
                    </StackPanel>
                </Button>
                <Button
                    x:Name="CloseBtn"
                    Click="Close_Click"
                    Content="{loc:Msg Key=Common.Close}"
                    Style="{StaticResource AccentButtonStyle}" />
            </StackPanel>
        </Grid>

    </Grid>
</UserControl>
