# セキュリティポリシー — ないみつクン（Naimitsu Vault）

<p align="center">
  <a href="SECURITY.md">English</a> | <strong>日本語</strong>
</p>

本文書は、ないみつクンのセキュリティに関する方針を定めます。
脆弱性の報告手順、守護する脅威の境界、および対象外の脅威を明示します。

---

## 脆弱性の報告

### 非公開報告の義務

発見された脆弱性を **GitHub の公開 Issue・Discussion・Pull Request に投稿しないでください。**
公開投稿は、修正前に攻撃者が問題を知る機会を生み出します。

### 報告窓口

GitHub の **Private Vulnerability Reporting**機能を使用してください。

1. [https://github.com/tabitos-atelier/naimitsu-vault/security/advisories/new](https://github.com/tabitos-atelier/naimitsu-vault/security/advisories/new) へアクセス
2. 「Report a vulnerability」から非公開で報告する

上記 URL が利用できない場合は、以下のメールアドレスへ直接連絡してください。

**連絡先**: tabitos.chalice@gmail.com

メールの件名は `[SECURITY] ないみつクン — <問題の概要>` としてください。

### 報告に含めていただきたい情報

* 脆弱性の種類（例: 暗号の誤実装・メモリ残留・認証バイパス）
* 影響を受けるバージョン・コンポーネント
* 再現手順（ステップ・コード・設定等）
* 攻撃者が得られる潜在的な利益（機密情報の漏洩・特権昇格等）
* 可能であれば、修正案または回避策

### 対応プロセス

| フェーズ                 | 目標期間             |
| -------------------- | ---------------- |
| 受領確認                 | 報告から 7 日以内       |
| 初期評価（重大度・再現確認）       | 受領から 14 日以内      |
| 修正リリース（重大度が高・致命的の場合） | 評価完了から 90 日以内を目標 |
| 公開開示                 | 修正リリース後、報告者と合意の上 |

個人開発のため、上記はベストエフォートです。商業製品と同等の SLA は提供できません。

---

## セキュリティの境界（守護する脅威）

ないみつクンが**設計上、対策を講じている**脅威の範囲を以下に定義します。

### 1. ローカル永続化データの暗号化とゼロディスク残留

**守護する脅威**: DB ファイル（`.nkdb`）の物理的な窃取・コピー、および一時ファイルの残渣解析

* すべての機密情報は **AES-256-GCM + Argon2id** で暗号化して SQLite に保存します。
* データ暗号化鍵（DEK）は、**Argon2id**（64MB メモリ・4並列・3反復）から導出した鍵暗号化鍵（KEK）でラップされます。GPU・ASIC による総当たり攻撃に対して、現実的な時間内での解読を著しく困難にします。
* DB を盗まれても、ユーザーの身元・行動を推測できるデータは平文で読み取れません（ゼロナレッジ設計）。
* **完全インメモリ直描画（ゼロディスク残留）**: 添付されたPDFや設定ファイルをビューワで閲覧する際、**ディスク上への平文一時ファイル（キャッシュ）の書き出しを一切行いません**。復号されたデータはメモリ上のみに展開され、閲覧終了またはロックと同時に完全に消去・解放されます。
* **TOTP QR コード取り込みの画面キャプチャ**: 認証アプリの QR コードをスクリーンキャプチャで読み取る機能は、キャプチャした画像をディスクへ書き出さずメモリ上のみで処理し、シークレットの解読後は `CryptographicOperations.ZeroMemory` で即座に消去します。
* **Windows Hello 連携時の端末バインド**: Windows Hello を有効にした場合、DEK・K_shared は Windows DPAPI（`ProtectedData`、`DataProtectionScope.CurrentUser`）でさらに暗号化した上で保存します。復号は同一 PC・同一 Windows アカウントでのみ可能なため、DB ファイル単体を別環境へ持ち出しても Windows Hello 経由での解錠はできません。

### 2. 入力データの水際全却下（異常終了防止・Fail-Safe）

**守護する脅威**: 不正なファイル・データのインポートによるランタイム汚染

* インポート処理はすべてのバイト列に対してエンコーディング検証・構造検証・整合性検証を実施します。
* 不正なデータは例外を表示して全却下し、アプリケーションを異常終了させず、DB を汚染させません（Fail-Safe 設計）。
* SQLite バイナリファイルのリストアは 3 層のバリデーション（マジックヘッダー・スキーマ・`PRAGMA integrity_check`）を通過した場合のみ実行されます。

### 3. 揮発メモリ空間のハイジーン

**守護する脅威**: DEK・パスワードのメモリスキャンによる露出

* DEK はロック時に `CryptographicOperations.ZeroMemory` でゼロクリアされます。
* パスワード・暗号鍵の中間バイト列は、使用直後にゼロクリアされます。
* **Pinned Object Heap（POH）によるメモリ移動残渣の防止**: 鍵材料・復号された平文を保持するバッファは `GC.AllocateArray(pinned: true)` により Pinned Object Heap に確保し、GC のコンパクション（メモリ移動）によって生じる移動前番地への残渣（GCゴースト）の残留を防止した上で、使用後は `CryptographicOperations.ZeroMemory` で即時消去します。
* AutoLock のデフォルトは 5 分で、無操作時に DEK を自動消去します。
* クリップボードへの書き込みは OS 履歴・クラウド同期から除外する方式（Win32 P/Invoke 経由）で行います。
* **クリップボード情報の30秒自動抹殺**: クリップボードにコピーされた機密情報は、**30秒後にバックグラウンドで自動消去**されます（CancellationTokenSource による時間制御）。平文の `string` インスタンスの生成を抑制し、メモリへの残渣残留リスクを最小限に抑えます。
* **クリップボードを経由しないキーボード自動入力（Direct Injection）**: 機密情報をクリップボードへコピーせずに直接キーストロークとして送信する自動入力機能を提供します。送信用の入力構造体はスタック上に確保し送信直後に即時ゼロ化するとともに、送信直前に送信先ウィンドウ（フォアグラウンド HWND）を検証し、意図しない別ウィンドウへの誤入力を防止します。
* **画面キャプチャ保護（OS レベルの撮影遮断）**: Windows API（`SetWindowDisplayAffinity`）により、スクリーンショット（PrintScreen、Snipping Tool）や画面共有ソフト（Discord、Teams 等）でウィンドウ内容が撮影できないよう自動的に保護します（既定で有効、設定画面で切り替え可能）。

### 4. 外部通信の境界（サイトアイコン自動取得）

**守護する脅威**: 登録先 Web サイトへのアクセスによる、利用者の IP アドレス・アクセス履歴の漏洩

* 「サイトアイコンの自動取得」を有効にした場合でも、登録先の Web サイトへ直接通信することは一切ありません。DuckDuckGo のアイコンキャッシュ API（`icons.duckduckgo.com`）経由でのみアイコン画像を取得します。
* キャッシュのキーには生のドメイン名ではなく HMAC-SHA256（DEK 由来）で計算したハッシュ値を使用し、ローカル DB にも平文のドメイン名を保存しません。
* 上記 API 側に未登録のドメインはアイコンを取得できません。この場合は静かに失敗し、キャッシュ未登録として扱われます（フォールバックによる直接アクセスは行いません）。
* 設定画面で本機能を無効化すれば、この経路の通信は完全にゼロになります。

---

## セキュリティの境界外（対象外の脅威）

以下の脅威は、本ソフトウェアの**設計上の対策範囲外**です。
これらの脅威に対しては、OS やハードウェアレベルの対策が必要です。

### 1. ホスト OS 自体の汚染

* **カーネル・ルート権限の奪取**: 攻撃者が管理者権限を持つ場合、プロセスのメモリを読み取ることができます。本ソフトウェアのメモリ保護は、OS がプロセス分離を正常に機能させていることを前提とします。
* **キーロガー**: ユーザーがマスターパスワードを入力する際にキーストロークを記録するマルウェアは、本ソフトウェアの保護範囲外です。
* **高度な画面キャプチャ・物理的撮影**: 管理者権限によるディスプレイドライバの直接フック、OS カーネルレベルでの画面読み取り、および外部カメラによる物理的な画面撮影は保護の対象外です。
* **プロセスメモリの直接ダンプ**: 管理者権限を持つプロセスによるメモリダンプは対策範囲外です。

### 2. 物理的な端末の強奪

* デバイスが起動した状態・ログイン済みの状態で物理的に強奪された場合、セッションが有効なうちはアンロック済みの金庫にアクセスできる可能性があります。
* AutoLock（5 分）が発動しているか、または手動ロック済みの状態であれば DEK は消去済みです。その状態での強奪に対しては、暗号化が保護として機能します。
* **ロック時のビューワ強制全閉鎖**: 自動ロックまたは手動ロック（`Ctrl + L`）が執行された瞬間、セッション破棄と完全に連動して、**現在開かれているすべての復号ビューワウィンドウが即座に強制全閉鎖**されます。これにより、離席時や強奪時における視覚的な情報漏洩を構造的に防ぎます。下書き比較画面（機密情報・プロフィール）等、ページ内に開かれた `ContentDialog` も同時に能動的にクローズされ、内部の平文バッファも消去されます（800ms のタイムアウト付き。ロック処理自体を停止させないため）。

### 3. ユーザー自身の不注意による漏洩

* マスターパスワードを他者に告知・記録の管理不備によって漏洩させた場合。
* エクスポートした平文ファイル（CSV/JSON）の管理不備による漏洩。
* 緊急アクセスコード（QR+PIN）の紛失・漏洩。

### 4. サプライチェーン攻撃

* 本ソフトウェアが依存する .NET ランタイム・NuGet パッケージ・Windows 自体に含まれる脆弱性。依存関係は最小限に抑えていますが、完全な排除は不可能です。

### 5. UI コントロール（WinUI 3）の内部ネイティブバッファ

* `PasswordBox`・`TextBox` などの標準 UI コントロールは、表示・Undo 履歴・IME 変換のために独自のネイティブ（WinRT）内部バッファを保持しており、マネージドコードから参照・消去する API は存在しません。
* マスターパスワード入力欄では、マネージド側コピー（`.Password` ゲッターが返す `string`・認証用 `char[]`）は使用直後に確実にゼロクリアしていますが、`PasswordBox` 自身が保持するネイティブバッファには一切干渉できません。解錠ウィンドウは試行のたびに新規生成されるため、この残留コピーは解錠試行の世代ごとに積み上がります。
* これは WinUI 3 フレームワークの構造的な制約であり、本ソフトウェア側で是正できる手段は現時点で存在しません。

---

## サポートされているバージョン

| バージョン      | セキュリティ対応               |
| ---------- | ---------------------- |
| 最新リリース     | ✅ 対応                   |
| 1 世代前のリリース | ケースバイケース（重大度により判断）     |
| それ以前       | ❌ 対応なし（最新版へのアップデートを推奨） |

---

## 本ソフトウェアの限界の誠実な開示

* **監査未実施**: 第三者の専門機関による包括的なセキュリティ監査は実施していません。Argon2id・AES-256-GCM は業界標準ですが、実装の正確性は著者の知識と自動テストに依存します。
* **個人開発**: 商業セキュリティ製品と比較して、対応リソース・専門性に限界があります。
* **無保証**: 本ソフトウェアは MIT ライセンスの条件で提供され、いかなる保証も行いません（`THE SOFTWARE IS PROVIDED "AS IS", WITHOUT WARRANTY OF ANY KIND`）。

**損失が許容不能な機密情報の管理には使用しないでください。**

---

## 謝辞

責任ある開示（Coordinated Vulnerability Disclosure）の原則に従って報告してくださった方には、修正後の公開 Security Advisory で報告者のお名前（またはハンドル名）を明記します（ご希望の場合のみ）。

---

*このポリシーは予告なく更新される場合があります。最新版はリポジトリで確認してください。*